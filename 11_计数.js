/*
题目描述
统计数组 arr 中值等于 item 的元素出现的次数
*/
/*
示例1
输入
[1, 2, 4, 4, 3, 4, 3], 4
输出
3
*/

/**
 * 思路一：
 * 利用 filter 过滤，过滤出与 item 相等的元素，返回的数组中都是与 item 相等的元素，最后只看 filter 返回新数组的长度即可。
 */

/**
 * 思路二：
 * sum 统计；
 * for 循环遍历。
 */